
with (OTransition) // Modify properties of this object
{
	mode = argument[0]; // sets mode to the first argument passed
	if (argument_count >1) target = argument[1];
	// If more than one argument is passed, set to the second argument, assign it to the target
}

