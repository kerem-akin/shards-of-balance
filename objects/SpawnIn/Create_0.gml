x = OPlayer.x;  // Position is in the same x-axis as player
y = OPlayer.y - 85;  // Position it 85 pixels above the player

image_speed = 1;  // Original animation speed

image_xscale = 1.5; // Scaled up x1.5
image_yscale = 1.5; // Scaled up x1.5
